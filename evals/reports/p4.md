# p4

model_id: claude-sonnet-4-6
repeats: 1
fixtures: 2
mean_brier: 0.17
mean_drop_rate: 0.06
mean_derived_only_rate: 0.00
mean_tell_only_rate: 0.00

## summary

| fixture | source | claims | drops | hit_rate | brier | drop_rate | jaccard |
|---|---|---|---|---|---|---|---|
| ios-lockdown-empty | sanitized | 7 | 1 | 1.00 | 0.13 | 0.13 | 1.00 |
| vpn-datacenter-mullvad | sanitized | 14 | 0 | 0.89 | 0.21 | 0.00 | 1.00 |

## reliability

| tier | n | hits | hit_rate |
|---|---|---|---|
| HUNCH | 1 | 1 | 1.00 |
| PLAUSIBLE | 2 | 2 | 1.00 |
| LIKELY | 10 | 9 | 0.90 |
| CONFIDENT | 1 | 1 | 1.00 |

## ios-lockdown-empty

source: sanitized
hit_rate: 1.00  brier: 0.13  drop_rate: 0.13  declined_rate: 0.53  jaccard: 1.00
derived_share: 0.13  derived_only_rate: 0.00  tell_only_rate: 0.00  raw_per_claim: 4.71
behavior_sparse: false

### claims
- `connection_context` LIKELY [hit] The visitor is connected through iCloud Private Relay, a relay/datacenter infrastructure — not a home broadband or mobile carrier connection.
- `device_tier` LIKELY [unlabeled] This is a current flagship-tier iPhone: P3 wide-gamut display, high dynamic range on both CSS and video, DPR 3, and iOS 18 running on hardware within the 14/15/16 generation bucket.
- `device_family` LIKELY [hit] The visitor is on an iPhone-class touch device: coarse pointer, no hover, portrait orientation, 393×852 screen at DPR 3, max_touch 5.
- `os_browser_posture` LIKELY [hit] iOS 18.0 running Safari 18.0 Mobile via WebKit; client hint headers are absent, which is expected for Safari and confirms no Chromium engine is present.
- `technical_sophistication` PLAUSIBLE [unlabeled] The visitor deliberately activated iCloud Private Relay (an iCloud+ paid feature) and is benefiting from iOS Safari's fingerprint suppression stack — indicating above-average privacy awareness, though both tools are consumer-grade and do not require deep technical knowledge.
- `language_profile` PLAUSIBLE [hit] English is the sole UI language, with no regional subtag and no secondary language present in either the header or the client list.
- `network_evasion` LIKELY [hit] iCloud Private Relay is active; the Amsterdam AMS colo is the egress node, not the person's location — but the relay did not cover device class, Apple GPU renderer, iOS 18 / Safari 18 UA, 393×852 DPR-3 P3+HDR display, or the bare English language tag.

### drops
- `privacy_posture` unknown_pointer (sig.client.intl.timezone)

### declined
- `location_region` The edge country (NL) is an iCloud Private Relay egress, not a person-location; timezone is null; all Intl fields are null; the language tag carries no regional marker. There is no surviving person-location signal to reason from.
- `location_precision` Edge city is null (relay); timezone and GPS are absent; no precision floor exists to state.
- `residency_status` No location signal survived the relay — mobility and tenure cannot be assessed without at least a region anchor.
- `time_context` Timezone is null and no local_time was derivable; the visit time cannot be placed in the visitor's day.
- `age_cohort` Owning a current-generation iPhone and running iOS 18 describes a large share of active iPhone users; no software-era narrowing is possible without font or application signals.
- `employment_sector` No ASN, font, or behavioral signal points toward a sector.
- `employer_or_org` No rDNS org, no proprietary probe-hit fonts, and no software_implied mapping is present.
- `visit_intent` No referrer and no behavioral divergence that would distinguish this visit from any other.
- `installed_software` fonts.probe_hits is empty and sig.derived.software_implied is absent; the rule requires at least one of these.

thin_signal_note: iCloud Private Relay concealed the IP address and suppressed the exit city. What it did not hide: iPhone 14/15/16-class body (393×852, DPR 3, max_touch 5), Apple GPU renderer string, iOS 18.0 / Safari 18.0 Mobile UA, P3 + HDR display capabilities, coarse-pointer touch form factor, and a bare 'en' language tag with no secondary language. The fingerprint surface is otherwise near-zero — ten collectors null — but those physical device signals are a coherent portrait on their own.

### missed_tells
- `asn_names_org` sig.edge.as_org is 'Apple Inc. iCloud Private Relay', which explicitly names Apple Inc. as the organization — a non-ISP org identity that this detector is designed to surface. It should have fired regardless of the relay context.

## vpn-datacenter-mullvad

source: sanitized
hit_rate: 0.89  brier: 0.21  drop_rate: 0.00  declined_rate: 0.18  jaccard: 1.00
derived_share: 0.15  derived_only_rate: 0.00  tell_only_rate: 0.00  raw_per_claim: 3.36
behavior_sparse: false

### claims
- `location_region` LIKELY [hit] The visitor is in the Netherlands; the Amsterdam timezone is preferred over the Mullvad New York exit as the person-region signal.
- `location_precision` HUNCH [hit] The visitor can be placed at country level (Netherlands) from timezone and locale, but not at city level — the only city in the signal set is a Mullvad exit.
- `residency_status` PLAUSIBLE [unlabeled] The device is configured as a Netherlands-region machine and the visitor shows no sign of recent relocation — they are present and settled in that locale, not travelling.
- `connection_context` CONFIDENT [hit] The visitor is behind a Mullvad VPN datacenter exit — this is not a home, office, or mobile ISP connection.
- `time_context` PLAUSIBLE [hit] The local time is 14:00 on a Friday in Amsterdam — mid-afternoon on a workday, consistent with a visit made during working hours.
- `device_tier` LIKELY [unlabeled] The device is a high-tier machine: Apple M-class GPU, 8 GB or more RAM, P3 wide-color gamut, and HDR-capable display.
- `device_family` LIKELY [hit] The visitor is on a desktop-class Mac with an Apple Silicon (M-class) GPU, reported at 1440×900 Retina density.
- `os_browser_posture` LIKELY [hit] The visitor runs Chrome 128 on macOS with client hints active and no browser-level fingerprint hardening.
- `privacy_posture` PLAUSIBLE [unlabeled] The visitor has a split privacy posture: network layer is hardened via Mullvad VPN, but the browser layer is entirely unmodified — fingerprint collectors returned real values across the board.
- `technical_sophistication` PLAUSIBLE [unlabeled] The visitor exhibits above-average technical awareness: they use a subscription VPN with deliberate exit selection and have developer tooling installed on their machine.
- `language_profile` LIKELY [miss] Dutch is the visitor's primary language; English is carried as a secondary language and is likely acquired rather than native.
- `employment_sector` HUNCH [unlabeled] The visitor is plausibly in the software or technology sector, given developer tooling and a weekday work-hours visit.
- `network_evasion` LIKELY [hit] The visitor routed through a Mullvad VPN exit in New York; the tunnel hid the IP address and nothing else — Europe/Amsterdam timezone and a Dutch-first language list arrived in the clear.
- `installed_software` LIKELY [hit] Apple Developer Tools (Xcode) is installed; SF Mono is the discriminating probe hit — it ships exclusively with that package.

### drops
- none

### declined
- `age_cohort` Apple M-class hardware and Chrome 128 are current-generation but span too wide a range to distinguish cohorts. No software-era signal specific enough to place an age bracket.
- `employer_or_org` No corporate or institutional ASN, no rDNS, and no proprietary employer font in the probe hits. software_implied names the toolchain (Apple Developer Tools), not an employer.
- `visit_intent` No behavioral signal (dwell map, scroll, pointer jitter pattern) distinguishes purposeful from incidental; any statement here would be Barnum.

thin_signal_note: The Mullvad tunnel concealed the IP address. It did not conceal the timezone (Europe/Amsterdam), the Dutch-first language list, the Monday-first week configuration, 24-hour clock, metric measurement, Apple Silicon device class, or real fingerprint collector values. The portrait rests on what walked past the exit, not on what the exit advertised.

### missed_tells
- `ua_vs_gpu` The UA string reports 'Intel Mac OS X 10_15_7' while the WebGL renderer identifies an Apple M-class (ARM) GPU — a platform mismatch between the UA's claimed architecture and the actual silicon. Chrome on Apple Silicon freezes this UA string by design, but the contradiction between 'Intel' in the UA and 'Apple M-class' in the WebGL renderer is a textbook ua_vs_gpu signal the detector should surface, even with a note that this is Chrome's standard behavior on M-series hardware.
